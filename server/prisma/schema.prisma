generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Exercise {
  exercise_name String   @db.VarChar
  set_id        Int
  lift_date     DateTime @db.Date
  reps          Int?
  weight        Float?
  unit          String?  @db.VarChar
  user_id       Int
  User          User     @relation(fields: [user_id], references: [user_id], onDelete: Cascade)

  @@id([exercise_name, set_id, lift_date, user_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Lift {
  lift_date  DateTime @db.Date
  lift_title String   @db.VarChar
  user_id    Int
  User       User     @relation(fields: [user_id], references: [user_id], onDelete: Cascade)

  @@id([lift_date, user_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Run {
  run_title String   @db.VarChar
  run_date  DateTime @db.Date
  distance  Float?   @db.Real
  time      Float?   @db.Real
  pace      Float?   @db.Real
  notes     String?  @db.VarChar
  unit      String?  @db.VarChar
  user_id   Int      @unique
  User      User     @relation(fields: [user_id], references: [user_id], onDelete: Cascade)

  @@id([run_date, user_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model User {
  email        String     @unique @db.VarChar
  password     String     @db.VarChar
  phone_number String?    @db.VarChar
  name         String?    @db.VarChar
  user_id      Int        @id @unique @default(autoincrement())
  Exercise     Exercise[]
  Lift         Lift[]
  Run          Run?
}
